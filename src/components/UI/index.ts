// Exportação dos componentes UI
export { default as Pagination } from './Pagination';

// Tipos/interfaces dos componentes (se necessário)
export type { default as PaginationProps } from './Pagination';